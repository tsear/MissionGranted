/**
 * CardNav Bundle Entry Point
 * Loaded on ALL pages for global navigation
 *
 * @package MissionGranted
 * @since 1.0.0
 */

import { initCardNav } from './components/initCardNav.js';

// Initialize CardNav on all pages
initCardNav();
